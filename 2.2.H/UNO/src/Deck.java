import java.util.List;

public class Deck {
    private final List<Card> cards;

    public Deck(List<Card> cards) {
        for number in Number:
            for color in Color:
                cards.add(Card(number, color));
    }
    public void shuffle() {
    }

    public Card drawCard() {

    }
}
